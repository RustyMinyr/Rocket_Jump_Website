import type { Metadata } from "next";
import { QuoteForm } from "@/components/QuoteForm";
import { PageHero } from "@/components/UI";
import { pageMetadata, siteConfig } from "@/lib/site";
export const metadata: Metadata = pageMetadata("Request a Website Quote in Gqeberha", "Tell RocketJump about your website, branding or social media project and request a quote in Gqeberha, Port Elizabeth or the Eastern Cape.", "/contact");
export default function ContactPage(){return <><PageHero eyebrow="START A PROJECT" title="Ready to launch?" copy="Based in Gqeberha and working with small businesses across Port Elizabeth and the Eastern Cape, RocketJump will recommend the right website, branding or social-media solution." accent="pink" visual="contact" ctaHref="#enquiry"/><section id="enquiry" className="section-light contact-section"><div className="shell contact-grid"><aside><span className="eyebrow">LET&apos;S TALK</span><h2>A clear first step.</h2><p>Share as much detail as you have. If you are not sure which package fits, that is exactly what we can help with.</p><div className="contact-card"><small>EMAIL</small><a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a><small>BASED IN</small><span>{siteConfig.location}</span></div></aside><QuoteForm/></div></section></>}
